简单实现了下 spinlock，即自旋锁，自旋锁和互斥锁一样，都是互斥的，
阻止多个线程同时进入临界区，不过互斥锁会阻塞睡眠后请求锁的人，
而自旋锁则会让后请求锁的人一直轮询判断自旋锁似乎解锁
自旋锁自然轮询是非常大的开销，因此几乎只在内核态一些地方使用
但是，并不是说互斥锁就一定好，互斥锁会导致阻塞唤醒，而这之间上下文切换
开销也不小，频繁的切换也会有很大的开销，因此要看具体的场景
一般在用户态用互斥锁，而内核态有些操作花费时间很短，上下文切换不值得，直接轮询反而更快，因此用自旋锁更好
